export default class GeminiConstants {
  static createPromptAddMeal(text: string) {
    console.log("🚀 ~ GeminiConstants ~ createPromptAddMeal ~ text:", text);
  }
}

// Aqui vai gerar um prompt padrão para cada serviço da api
